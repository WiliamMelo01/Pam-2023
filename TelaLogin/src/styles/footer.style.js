import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    alignItems: "center",
    marginTop: 70,
  },
  copy: {
    color: "white",
    fontSize: 14,
    fontFamily: "Poppins-Bold",
  },
});
